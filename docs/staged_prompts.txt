AsyncDataLoader 分阶段新对话提示词全集（独立可执行版）

使用方式：
1. 每个阶段单独开一个新对话。
2. 新对话开始时上传项目执行手册、本阶段前一阶段交接总结，以及当前关键代码文件。
3. 复制对应阶段的提示词。
4. 阶段结束后，让 AI 按固定格式生成交接总结，保存到 docs/stage_summaries/stageX_summary.md。
5. 本地 git commit 后再进入下一阶段。

项目最终定位：
AsyncDataLoader 是一个面向科学计算离线数据加工场景的大规模数据异步预处理流水线。它的核心流程是读取原始大文件数据块，执行 CPU 预处理 Stage，再写入处理后的新文件。项目重点不是某个学科算法，而是用 Linux io_uring、C++20 协程、IOBackend fallback、Stage 注册、BufferPool、Metrics、Benchmark 和错误测试，把串行 read-process-write 流程改造成可配置、可观测、可验证的工程系统。

通用阶段结束提示词：
请生成本阶段交接总结，要求包括：
1. 当前阶段完成了什么；
2. 当前目录结构；
3. 新增/修改了哪些文件；
4. 每个文件的作用；
5. 当前能运行的命令；
6. 当前测试结果；
7. 遇到过哪些 bug，怎么解决；
8. 还没解决的问题；
9. 下一阶段要做什么；
10. 面试时这一阶段可以怎么讲。

==============================



============================================================
阶段 0：环境准备与项目骨架
============================================================

【阶段 0：环境准备与项目骨架】

开新对话时复制以下内容：

我正在做一个 C++ 秋招项目 AsyncDataLoader。项目定位如下：
AsyncDataLoader 是一个面向科学计算离线数据加工场景的大规模数据异步预处理流水线。它不依赖任何特定学科数据格式，核心流程是：读取原始大文件数据块 -> 执行 CPU 预处理 Stage -> 写入处理后的新文件。项目重点是用 io_uring、C++20 协程、IOBackend fallback、Stage 注册、BufferPool、Metrics 和 Benchmark，把串行 read-process-write 流程改造成可配置、可观测、可测试的流水线。

当前我要做的是：阶段 0：环境准备与项目骨架。

请严格只完成这一阶段，不扩展新方向，不讨论选题，不引入 CUDA、AI Infra、分布式存储、地震专用格式或 FWI checkpoint。

本阶段目标：
搭建 CMake + C++20 项目；确认 liburing 可用；建立 docs、include、src、tests、benchmark、examples 目录；创建 README、design.md、benchmark.md、interview.md。

本阶段必须产出：
CMakeLists.txt；最小 hello 可编译；项目目录；README 和 docs 空模板；首次 git commit 建议。

要求：
1. 先解释本阶段在整个项目中的位置。
2. 给出最小可运行代码，不要一次生成整个项目。
3. 每个文件、类、函数和关键变量都要解释。
4. 给出编译命令、运行命令和预期输出。
5. 给出常见 bug、排查方法和面试问答。
6. 阶段结束时，生成交接总结，方便我开新对话继续。


阶段结束后，请输出以下固定格式的交接总结：
1. 当前阶段完成了什么；
2. 当前目录结构；
3. 新增/修改了哪些文件；
4. 每个文件的作用；
5. 当前能运行的命令；
6. 当前测试结果；
7. 遇到过哪些 bug，怎么解决；
8. 还没解决的问题；
9. 下一阶段要做什么；
10. 面试时这一阶段可以怎么讲。



============================================================
阶段 1：C++20 协程专项学习
============================================================

【阶段 1：C++20 协程专项学习】

开新对话时复制以下内容：

我正在做一个 C++ 秋招项目 AsyncDataLoader。项目定位如下：
AsyncDataLoader 是一个面向科学计算离线数据加工场景的大规模数据异步预处理流水线。它不依赖任何特定学科数据格式，核心流程是：读取原始大文件数据块 -> 执行 CPU 预处理 Stage -> 写入处理后的新文件。项目重点是用 io_uring、C++20 协程、IOBackend fallback、Stage 注册、BufferPool、Metrics 和 Benchmark，把串行 read-process-write 流程改造成可配置、可观测、可测试的流水线。

当前我要做的是：阶段 1：C++20 协程专项学习。

请严格只完成这一阶段，不扩展新方向，不讨论选题，不引入 CUDA、AI Infra、分布式存储、地震专用格式或 FWI checkpoint。

本阶段目标：
不接 io_uring，先学透协程。理解 Task<T>、promise_type、coroutine_handle、await_ready、await_suspend、await_resume、initial_suspend、final_suspend、异常传播和生命周期。

本阶段必须产出：
simple_task_demo；manual_resume_demo；delay_awaiter_demo；协程数据流图；docs/interview.md 中的协程问答。

要求：
1. 先解释本阶段在整个项目中的位置。
2. 给出最小可运行代码，不要一次生成整个项目。
3. 每个文件、类、函数和关键变量都要解释。
4. 给出编译命令、运行命令和预期输出。
5. 给出常见 bug、排查方法和面试问答。
6. 阶段结束时，生成交接总结，方便我开新对话继续。


阶段结束后，请输出以下固定格式的交接总结：
1. 当前阶段完成了什么；
2. 当前目录结构；
3. 新增/修改了哪些文件；
4. 每个文件的作用；
5. 当前能运行的命令；
6. 当前测试结果；
7. 遇到过哪些 bug，怎么解决；
8. 还没解决的问题；
9. 下一阶段要做什么；
10. 面试时这一阶段可以怎么讲。



============================================================
阶段 2：Linux 文件 I/O 与同步基线
============================================================

【阶段 2：Linux 文件 I/O 与同步基线】

开新对话时复制以下内容：

我正在做一个 C++ 秋招项目 AsyncDataLoader。项目定位如下：
AsyncDataLoader 是一个面向科学计算离线数据加工场景的大规模数据异步预处理流水线。它不依赖任何特定学科数据格式，核心流程是：读取原始大文件数据块 -> 执行 CPU 预处理 Stage -> 写入处理后的新文件。项目重点是用 io_uring、C++20 协程、IOBackend fallback、Stage 注册、BufferPool、Metrics 和 Benchmark，把串行 read-process-write 流程改造成可配置、可观测、可测试的流水线。

当前我要做的是：阶段 2：Linux 文件 I/O 与同步基线。

请严格只完成这一阶段，不扩展新方向，不讨论选题，不引入 CUDA、AI Infra、分布式存储、地震专用格式或 FWI checkpoint。

本阶段目标：
学习 open、close、read、write、pread、pwrite、fsync、errno、page cache、O_DIRECT 基本概念；实现 RAII FdGuard；实现同步 read-process-write baseline。

本阶段必须产出：
FdGuard；sync_preprocess_baseline.cpp；Timer；能读取原始文件块、做简单处理、写输出文件。

要求：
1. 先解释本阶段在整个项目中的位置。
2. 给出最小可运行代码，不要一次生成整个项目。
3. 每个文件、类、函数和关键变量都要解释。
4. 给出编译命令、运行命令和预期输出。
5. 给出常见 bug、排查方法和面试问答。
6. 阶段结束时，生成交接总结，方便我开新对话继续。


阶段结束后，请输出以下固定格式的交接总结：
1. 当前阶段完成了什么；
2. 当前目录结构；
3. 新增/修改了哪些文件；
4. 每个文件的作用；
5. 当前能运行的命令；
6. 当前测试结果；
7. 遇到过哪些 bug，怎么解决；
8. 还没解决的问题；
9. 下一阶段要做什么；
10. 面试时这一阶段可以怎么讲。



============================================================
阶段 3：Benchmark 框架与 mmap 对比基线
============================================================

【阶段 3：Benchmark 框架与 mmap 对比基线】

开新对话时复制以下内容：

我正在做一个 C++ 秋招项目 AsyncDataLoader。项目定位如下：
AsyncDataLoader 是一个面向科学计算离线数据加工场景的大规模数据异步预处理流水线。它不依赖任何特定学科数据格式，核心流程是：读取原始大文件数据块 -> 执行 CPU 预处理 Stage -> 写入处理后的新文件。项目重点是用 io_uring、C++20 协程、IOBackend fallback、Stage 注册、BufferPool、Metrics 和 Benchmark，把串行 read-process-write 流程改造成可配置、可观测、可测试的流水线。

当前我要做的是：阶段 3：Benchmark 框架与 mmap 对比基线。

请严格只完成这一阶段，不扩展新方向，不讨论选题，不引入 CUDA、AI Infra、分布式存储、地震专用格式或 FWI checkpoint。

本阶段目标：
建立可复用 benchmark 框架，支持吞吐、平均延迟、P50/P95/P99、端到端耗时；加入 sync pread 和 mmap sequential scan 对比。

本阶段必须产出：
bench_sync；bench_mmap；CSV 输出；benchmark.md 记录模板；公平测试说明。

要求：
1. 先解释本阶段在整个项目中的位置。
2. 给出最小可运行代码，不要一次生成整个项目。
3. 每个文件、类、函数和关键变量都要解释。
4. 给出编译命令、运行命令和预期输出。
5. 给出常见 bug、排查方法和面试问答。
6. 阶段结束时，生成交接总结，方便我开新对话继续。


阶段结束后，请输出以下固定格式的交接总结：
1. 当前阶段完成了什么；
2. 当前目录结构；
3. 新增/修改了哪些文件；
4. 每个文件的作用；
5. 当前能运行的命令；
6. 当前测试结果；
7. 遇到过哪些 bug，怎么解决；
8. 还没解决的问题；
9. 下一阶段要做什么；
10. 面试时这一阶段可以怎么讲。



============================================================
阶段 4：原生 liburing 基础
============================================================

【阶段 4：原生 liburing 基础】

开新对话时复制以下内容：

我正在做一个 C++ 秋招项目 AsyncDataLoader。项目定位如下：
AsyncDataLoader 是一个面向科学计算离线数据加工场景的大规模数据异步预处理流水线。它不依赖任何特定学科数据格式，核心流程是：读取原始大文件数据块 -> 执行 CPU 预处理 Stage -> 写入处理后的新文件。项目重点是用 io_uring、C++20 协程、IOBackend fallback、Stage 注册、BufferPool、Metrics 和 Benchmark，把串行 read-process-write 流程改造成可配置、可观测、可测试的流水线。

当前我要做的是：阶段 4：原生 liburing 基础。

请严格只完成这一阶段，不扩展新方向，不讨论选题，不引入 CUDA、AI Infra、分布式存储、地震专用格式或 FWI checkpoint。

本阶段目标：
不用协程，先实现 liburing 单次 read、批量 read 和基本 write。理解 SQ、SQE、CQ、CQE、user_data、submit、wait_cqe、错误码。

本阶段必须产出：
uring_read_once；uring_batch_read；uring_write_demo；能解释 CQE res 和 user_data。

要求：
1. 先解释本阶段在整个项目中的位置。
2. 给出最小可运行代码，不要一次生成整个项目。
3. 每个文件、类、函数和关键变量都要解释。
4. 给出编译命令、运行命令和预期输出。
5. 给出常见 bug、排查方法和面试问答。
6. 阶段结束时，生成交接总结，方便我开新对话继续。


阶段结束后，请输出以下固定格式的交接总结：
1. 当前阶段完成了什么；
2. 当前目录结构；
3. 新增/修改了哪些文件；
4. 每个文件的作用；
5. 当前能运行的命令；
6. 当前测试结果；
7. 遇到过哪些 bug，怎么解决；
8. 还没解决的问题；
9. 下一阶段要做什么；
10. 面试时这一阶段可以怎么讲。



============================================================
阶段 5：io_uring 与 C++20 协程结合
============================================================

【阶段 5：io_uring 与 C++20 协程结合】

开新对话时复制以下内容：

我正在做一个 C++ 秋招项目 AsyncDataLoader。项目定位如下：
AsyncDataLoader 是一个面向科学计算离线数据加工场景的大规模数据异步预处理流水线。它不依赖任何特定学科数据格式，核心流程是：读取原始大文件数据块 -> 执行 CPU 预处理 Stage -> 写入处理后的新文件。项目重点是用 io_uring、C++20 协程、IOBackend fallback、Stage 注册、BufferPool、Metrics 和 Benchmark，把串行 read-process-write 流程改造成可配置、可观测、可测试的流水线。

当前我要做的是：阶段 5：io_uring 与 C++20 协程结合。

请严格只完成这一阶段，不扩展新方向，不讨论选题，不引入 CUDA、AI Infra、分布式存储、地震专用格式或 FWI checkpoint。

本阶段目标：
把 io_uring 完成事件和 coroutine_handle 接起来，实现 co_await read_at。重点处理 Request 生命周期、buffer 生命周期、错误传播、只 resume 一次。

本阶段必须产出：
Task<T>；ReadAwaiter；UringBackend::read_at；async_read_demo；一张 co_await read_at 数据流说明。

要求：
1. 先解释本阶段在整个项目中的位置。
2. 给出最小可运行代码，不要一次生成整个项目。
3. 每个文件、类、函数和关键变量都要解释。
4. 给出编译命令、运行命令和预期输出。
5. 给出常见 bug、排查方法和面试问答。
6. 阶段结束时，生成交接总结，方便我开新对话继续。


阶段结束后，请输出以下固定格式的交接总结：
1. 当前阶段完成了什么；
2. 当前目录结构；
3. 新增/修改了哪些文件；
4. 每个文件的作用；
5. 当前能运行的命令；
6. 当前测试结果；
7. 遇到过哪些 bug，怎么解决；
8. 还没解决的问题；
9. 下一阶段要做什么；
10. 面试时这一阶段可以怎么讲。



============================================================
阶段 6：IOBackend 抽象与三层 fallback
============================================================

【阶段 6：IOBackend 抽象与三层 fallback】

开新对话时复制以下内容：

我正在做一个 C++ 秋招项目 AsyncDataLoader。项目定位如下：
AsyncDataLoader 是一个面向科学计算离线数据加工场景的大规模数据异步预处理流水线。它不依赖任何特定学科数据格式，核心流程是：读取原始大文件数据块 -> 执行 CPU 预处理 Stage -> 写入处理后的新文件。项目重点是用 io_uring、C++20 协程、IOBackend fallback、Stage 注册、BufferPool、Metrics 和 Benchmark，把串行 read-process-write 流程改造成可配置、可观测、可测试的流水线。

当前我要做的是：阶段 6：IOBackend 抽象与三层 fallback。

请严格只完成这一阶段，不扩展新方向，不讨论选题，不引入 CUDA、AI Infra、分布式存储、地震专用格式或 FWI checkpoint。

本阶段目标：
设计统一 IOBackend 接口，支持 UringBackend、ThreadPoolBackend 和 SyncBackend。通过配置选择 backend；io_uring 不可用时回退。

本阶段必须产出：
IOBackend；UringBackend；ThreadPoolBackend；SyncBackend；BackendFactory；backend_fallback_demo。

要求：
1. 先解释本阶段在整个项目中的位置。
2. 给出最小可运行代码，不要一次生成整个项目。
3. 每个文件、类、函数和关键变量都要解释。
4. 给出编译命令、运行命令和预期输出。
5. 给出常见 bug、排查方法和面试问答。
6. 阶段结束时，生成交接总结，方便我开新对话继续。


阶段结束后，请输出以下固定格式的交接总结：
1. 当前阶段完成了什么；
2. 当前目录结构；
3. 新增/修改了哪些文件；
4. 每个文件的作用；
5. 当前能运行的命令；
6. 当前测试结果；
7. 遇到过哪些 bug，怎么解决；
8. 还没解决的问题；
9. 下一阶段要做什么；
10. 面试时这一阶段可以怎么讲。



============================================================
阶段 7：Stage 注册机制与 Pipeline 框架
============================================================

【阶段 7：Stage 注册机制与 Pipeline 框架】

开新对话时复制以下内容：

我正在做一个 C++ 秋招项目 AsyncDataLoader。项目定位如下：
AsyncDataLoader 是一个面向科学计算离线数据加工场景的大规模数据异步预处理流水线。它不依赖任何特定学科数据格式，核心流程是：读取原始大文件数据块 -> 执行 CPU 预处理 Stage -> 写入处理后的新文件。项目重点是用 io_uring、C++20 协程、IOBackend fallback、Stage 注册、BufferPool、Metrics 和 Benchmark，把串行 read-process-write 流程改造成可配置、可观测、可测试的流水线。

当前我要做的是：阶段 7：Stage 注册机制与 Pipeline 框架。

请严格只完成这一阶段，不扩展新方向，不讨论选题，不引入 CUDA、AI Infra、分布式存储、地震专用格式或 FWI checkpoint。

本阶段目标：
实现可扩展 Stage 注册。用户可以 add_stage 注册 CPU 处理函数。建立 read -> stage1 -> stage2 -> write 的数据流。

本阶段必须产出：
Stage 接口；Pipeline::add_stage；内置 NoOpStage、NormalizeStage、ChecksumStage；custom_stage_demo。

要求：
1. 先解释本阶段在整个项目中的位置。
2. 给出最小可运行代码，不要一次生成整个项目。
3. 每个文件、类、函数和关键变量都要解释。
4. 给出编译命令、运行命令和预期输出。
5. 给出常见 bug、排查方法和面试问答。
6. 阶段结束时，生成交接总结，方便我开新对话继续。


阶段结束后，请输出以下固定格式的交接总结：
1. 当前阶段完成了什么；
2. 当前目录结构；
3. 新增/修改了哪些文件；
4. 每个文件的作用；
5. 当前能运行的命令；
6. 当前测试结果；
7. 遇到过哪些 bug，怎么解决；
8. 还没解决的问题；
9. 下一阶段要做什么；
10. 面试时这一阶段可以怎么讲。



============================================================
阶段 8：BufferPool、PipelineConfig 与背压控制
============================================================

【阶段 8：BufferPool、PipelineConfig 与背压控制】

开新对话时复制以下内容：

我正在做一个 C++ 秋招项目 AsyncDataLoader。项目定位如下：
AsyncDataLoader 是一个面向科学计算离线数据加工场景的大规模数据异步预处理流水线。它不依赖任何特定学科数据格式，核心流程是：读取原始大文件数据块 -> 执行 CPU 预处理 Stage -> 写入处理后的新文件。项目重点是用 io_uring、C++20 协程、IOBackend fallback、Stage 注册、BufferPool、Metrics 和 Benchmark，把串行 read-process-write 流程改造成可配置、可观测、可测试的流水线。

当前我要做的是：阶段 8：BufferPool、PipelineConfig 与背压控制。

请严格只完成这一阶段，不扩展新方向，不讨论选题，不引入 CUDA、AI Infra、分布式存储、地震专用格式或 FWI checkpoint。

本阶段目标：
实现 AlignedBufferPool、BufferHandle RAII 归还、PipelineConfig、SPSC 队列和 back pressure。第一版可以用 mutex + condition_variable，正确性优先。

本阶段必须产出：
AlignedBufferPool；PipelineConfig；SPSCQueue；back pressure demo；O_DIRECT 对齐说明。

要求：
1. 先解释本阶段在整个项目中的位置。
2. 给出最小可运行代码，不要一次生成整个项目。
3. 每个文件、类、函数和关键变量都要解释。
4. 给出编译命令、运行命令和预期输出。
5. 给出常见 bug、排查方法和面试问答。
6. 阶段结束时，生成交接总结，方便我开新对话继续。


阶段结束后，请输出以下固定格式的交接总结：
1. 当前阶段完成了什么；
2. 当前目录结构；
3. 新增/修改了哪些文件；
4. 每个文件的作用；
5. 当前能运行的命令；
6. 当前测试结果；
7. 遇到过哪些 bug，怎么解决；
8. 还没解决的问题；
9. 下一阶段要做什么；
10. 面试时这一阶段可以怎么讲。



============================================================
阶段 9：Metrics 数据结构与自动埋点
============================================================

【阶段 9：Metrics 数据结构与自动埋点】

开新对话时复制以下内容：

我正在做一个 C++ 秋招项目 AsyncDataLoader。项目定位如下：
AsyncDataLoader 是一个面向科学计算离线数据加工场景的大规模数据异步预处理流水线。它不依赖任何特定学科数据格式，核心流程是：读取原始大文件数据块 -> 执行 CPU 预处理 Stage -> 写入处理后的新文件。项目重点是用 io_uring、C++20 协程、IOBackend fallback、Stage 注册、BufferPool、Metrics 和 Benchmark，把串行 read-process-write 流程改造成可配置、可观测、可测试的流水线。

当前我要做的是：阶段 9：Metrics 数据结构与自动埋点。

请严格只完成这一阶段，不扩展新方向，不讨论选题，不引入 CUDA、AI Infra、分布式存储、地震专用格式或 FWI checkpoint。

本阶段目标：
只做 Metrics 数据结构和 pipeline 自动埋点，不做 Dashboard。记录 counter、gauge、histogram、stage latency、throughput、queue depth。

本阶段必须产出：
MetricsRegistry；Counter/Gauge/Histogram；Stage 自动计时；terminal metrics 输出基础。

要求：
1. 先解释本阶段在整个项目中的位置。
2. 给出最小可运行代码，不要一次生成整个项目。
3. 每个文件、类、函数和关键变量都要解释。
4. 给出编译命令、运行命令和预期输出。
5. 给出常见 bug、排查方法和面试问答。
6. 阶段结束时，生成交接总结，方便我开新对话继续。


阶段结束后，请输出以下固定格式的交接总结：
1. 当前阶段完成了什么；
2. 当前目录结构；
3. 新增/修改了哪些文件；
4. 每个文件的作用；
5. 当前能运行的命令；
6. 当前测试结果；
7. 遇到过哪些 bug，怎么解决；
8. 还没解决的问题；
9. 下一阶段要做什么；
10. 面试时这一阶段可以怎么讲。



============================================================
阶段 10：端到端预处理流水线 Demo
============================================================

【阶段 10：端到端预处理流水线 Demo】

开新对话时复制以下内容：

我正在做一个 C++ 秋招项目 AsyncDataLoader。项目定位如下：
AsyncDataLoader 是一个面向科学计算离线数据加工场景的大规模数据异步预处理流水线。它不依赖任何特定学科数据格式，核心流程是：读取原始大文件数据块 -> 执行 CPU 预处理 Stage -> 写入处理后的新文件。项目重点是用 io_uring、C++20 协程、IOBackend fallback、Stage 注册、BufferPool、Metrics 和 Benchmark，把串行 read-process-write 流程改造成可配置、可观测、可测试的流水线。

当前我要做的是：阶段 10：端到端预处理流水线 Demo。

请严格只完成这一阶段，不扩展新方向，不讨论选题，不引入 CUDA、AI Infra、分布式存储、地震专用格式或 FWI checkpoint。

本阶段目标：
实现完整离线数据加工流程：读取原始数据块 -> Stage 预处理 -> 写入新文件。展示进度、吞吐、队列深度和每个 Stage 耗时。

本阶段必须产出：
preprocess_pipeline_demo；可选 backend；实时 terminal 输出；输出文件校验；不是“文件读取器”的证据。

要求：
1. 先解释本阶段在整个项目中的位置。
2. 给出最小可运行代码，不要一次生成整个项目。
3. 每个文件、类、函数和关键变量都要解释。
4. 给出编译命令、运行命令和预期输出。
5. 给出常见 bug、排查方法和面试问答。
6. 阶段结束时，生成交接总结，方便我开新对话继续。


阶段结束后，请输出以下固定格式的交接总结：
1. 当前阶段完成了什么；
2. 当前目录结构；
3. 新增/修改了哪些文件；
4. 每个文件的作用；
5. 当前能运行的命令；
6. 当前测试结果；
7. 遇到过哪些 bug，怎么解决；
8. 还没解决的问题；
9. 下一阶段要做什么；
10. 面试时这一阶段可以怎么讲。



============================================================
阶段 11：完整 Benchmark 与性能分析
============================================================

【阶段 11：完整 Benchmark 与性能分析】

开新对话时复制以下内容：

我正在做一个 C++ 秋招项目 AsyncDataLoader。项目定位如下：
AsyncDataLoader 是一个面向科学计算离线数据加工场景的大规模数据异步预处理流水线。它不依赖任何特定学科数据格式，核心流程是：读取原始大文件数据块 -> 执行 CPU 预处理 Stage -> 写入处理后的新文件。项目重点是用 io_uring、C++20 协程、IOBackend fallback、Stage 注册、BufferPool、Metrics 和 Benchmark，把串行 read-process-write 流程改造成可配置、可观测、可测试的流水线。

当前我要做的是：阶段 11：完整 Benchmark 与性能分析。

请严格只完成这一阶段，不扩展新方向，不讨论选题，不引入 CUDA、AI Infra、分布式存储、地震专用格式或 FWI checkpoint。

本阶段目标：
对比 sync、mmap、threadpool、io_uring、io_uring pipeline。寻找真实性能发现，不强行证明 io_uring 总是最快。用 strace/perf 做辅助分析。

本阶段必须产出：
完整 benchmark.md；结果表格；CSV；图表脚本；技术选型分析；反直觉发现记录。

要求：
1. 先解释本阶段在整个项目中的位置。
2. 给出最小可运行代码，不要一次生成整个项目。
3. 每个文件、类、函数和关键变量都要解释。
4. 给出编译命令、运行命令和预期输出。
5. 给出常见 bug、排查方法和面试问答。
6. 阶段结束时，生成交接总结，方便我开新对话继续。


阶段结束后，请输出以下固定格式的交接总结：
1. 当前阶段完成了什么；
2. 当前目录结构；
3. 新增/修改了哪些文件；
4. 每个文件的作用；
5. 当前能运行的命令；
6. 当前测试结果；
7. 遇到过哪些 bug，怎么解决；
8. 还没解决的问题；
9. 下一阶段要做什么；
10. 面试时这一阶段可以怎么讲。



============================================================
阶段 12：Terminal 美化输出与可选 JSON 指标接口
============================================================

【阶段 12：Terminal 美化输出与可选 JSON 指标接口】

开新对话时复制以下内容：

我正在做一个 C++ 秋招项目 AsyncDataLoader。项目定位如下：
AsyncDataLoader 是一个面向科学计算离线数据加工场景的大规模数据异步预处理流水线。它不依赖任何特定学科数据格式，核心流程是：读取原始大文件数据块 -> 执行 CPU 预处理 Stage -> 写入处理后的新文件。项目重点是用 io_uring、C++20 协程、IOBackend fallback、Stage 注册、BufferPool、Metrics 和 Benchmark，把串行 read-process-write 流程改造成可配置、可观测、可测试的流水线。

当前我要做的是：阶段 12：Terminal 美化输出与可选 JSON 指标接口。

请严格只完成这一阶段，不扩展新方向，不讨论选题，不引入 CUDA、AI Infra、分布式存储、地震专用格式或 FWI checkpoint。

本阶段目标：
优先做 terminal 状态输出；可选提供 /metrics JSON 文件或轻量 HTTP JSON 接口。不强做 Dashboard。

本阶段必须产出：
清晰 terminal 输出；metrics.json 可选；运行时状态展示；可演示项目形态。

要求：
1. 先解释本阶段在整个项目中的位置。
2. 给出最小可运行代码，不要一次生成整个项目。
3. 每个文件、类、函数和关键变量都要解释。
4. 给出编译命令、运行命令和预期输出。
5. 给出常见 bug、排查方法和面试问答。
6. 阶段结束时，生成交接总结，方便我开新对话继续。


阶段结束后，请输出以下固定格式的交接总结：
1. 当前阶段完成了什么；
2. 当前目录结构；
3. 新增/修改了哪些文件；
4. 每个文件的作用；
5. 当前能运行的命令；
6. 当前测试结果；
7. 遇到过哪些 bug，怎么解决；
8. 还没解决的问题；
9. 下一阶段要做什么；
10. 面试时这一阶段可以怎么讲。



============================================================
阶段 13：错误测试、文档与面试准备
============================================================

【阶段 13：错误测试、文档与面试准备】

开新对话时复制以下内容：

我正在做一个 C++ 秋招项目 AsyncDataLoader。项目定位如下：
AsyncDataLoader 是一个面向科学计算离线数据加工场景的大规模数据异步预处理流水线。它不依赖任何特定学科数据格式，核心流程是：读取原始大文件数据块 -> 执行 CPU 预处理 Stage -> 写入处理后的新文件。项目重点是用 io_uring、C++20 协程、IOBackend fallback、Stage 注册、BufferPool、Metrics 和 Benchmark，把串行 read-process-write 流程改造成可配置、可观测、可测试的流水线。

当前我要做的是：阶段 13：错误测试、文档与面试准备。

请严格只完成这一阶段，不扩展新方向，不讨论选题，不引入 CUDA、AI Infra、分布式存储、地震专用格式或 FWI checkpoint。

本阶段目标：
补错误测试和最终文档。测试 EOF、文件不存在、backend fallback、O_DIRECT 对齐错误、buffer 生命周期。整理 README、design.md、benchmark.md、interview.md。

本阶段必须产出：
错误测试；README；design.md；benchmark.md；interview.md；1 分钟/3 分钟项目介绍；简历表达。

要求：
1. 先解释本阶段在整个项目中的位置。
2. 给出最小可运行代码，不要一次生成整个项目。
3. 每个文件、类、函数和关键变量都要解释。
4. 给出编译命令、运行命令和预期输出。
5. 给出常见 bug、排查方法和面试问答。
6. 阶段结束时，生成交接总结，方便我开新对话继续。


阶段结束后，请输出以下固定格式的交接总结：
1. 当前阶段完成了什么；
2. 当前目录结构；
3. 新增/修改了哪些文件；
4. 每个文件的作用；
5. 当前能运行的命令；
6. 当前测试结果；
7. 遇到过哪些 bug，怎么解决；
8. 还没解决的问题；
9. 下一阶段要做什么；
10. 面试时这一阶段可以怎么讲。

